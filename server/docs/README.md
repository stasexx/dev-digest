# docs — server

Deep-dives for the `server` package (pipelines, diagrams, design notes).
`server/CLAUDE.md` links here via *Use when*.

- [architecture.md](architecture.md) — bootstrap, DI container + adapters, module
  registry and anatomy, error handling, config keys, DB schema + migration
  workflow, vendored contracts, repo-intel in brief, test layout.
