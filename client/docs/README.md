# docs — client

Deep-dives for the `client` package (pipelines, diagrams, design notes).
`client/CLAUDE.md` links here via *Use when*.

- [`ui-architecture.md`](ui-architecture.md) — route tree, Server/Client
  boundary, data layer (api + react-query hooks), vendored `@devdigest/ui` /
  `@devdigest/shared`, i18n, styling, component folder convention, testing.
