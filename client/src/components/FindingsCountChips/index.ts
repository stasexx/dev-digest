/* FindingsCountChips — per-severity count chips (icon + number) for the PR list
   FINDINGS column and the Agent-runs timeline rows. */
export { FindingsCountChips, type SeverityCounts } from "./FindingsCountChips";
export { countBySeverity, totalCount } from "./helpers";
