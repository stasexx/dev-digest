# specs — client

Specs / acceptance criteria for the `client` package.

- [`pages.md`](pages.md) — behavioural contract per route: data (hook +
  endpoint), URL params, loading/empty/error states, PR detail tabs, PR list
  columns.
